{% extends 'base.html.twig' %}

{% block title %}Hello LandingController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    This friendly message is coming from: LANDING PAGE BENJi LE SANG LE PLUS DOUX AIME MOI-> <a href="{{ path('meta_index') }}">Gerer les meta</a>
    <a href="{{ path('article_index') }}">Gerer les produits</a>

    {% for article in articles %}
        {{ article.title }}
        {{ article.excerpt }}
        {{ article.price }}
    {% endfor %}
{% endblock %}
